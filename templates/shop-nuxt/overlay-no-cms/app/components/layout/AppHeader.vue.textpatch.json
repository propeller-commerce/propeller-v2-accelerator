{
  "_comment": "No-CMS scaffold: the blog pages are removed, so drop the '/blog' entry from the header nav links — otherwise the menu shows a link to a 404. Surgical find/replace so the rest of this 400+ line header tracks the boilerplate. Anchored on the Blog entry alone: the surrounding declaration changed from `ref([...])` to `computed(() => {...})` when the contact-only Machines link landed, so don't re-anchor on it.",
  "replace": [
    {
      "find": "    { label: 'Blog', url: '/blog', highlight: false },\n",
      "with": ""
    }
  ]
}
