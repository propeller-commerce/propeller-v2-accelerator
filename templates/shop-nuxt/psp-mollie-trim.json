{
  "_comment": "Paths to delete when psp === 'mollie': the MultiSafepay half of the boilerplate's two-PSP surface. Its npm package is dropped from package.json by overlay-psp-mollie/package.patch.json. Safe in this direction with no extra patching: the MSP route imports from utils/mollie (for the shared on-account guard), never the reverse.",
  "remove": [
    "server/api/msp",
    "server/utils/msp.ts"
  ]
}
